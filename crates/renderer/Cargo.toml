[package]
name = "fluxel-renderer"
version.workspace = true
edition = "2024"
rust-version = "1.87"
license = "MIT OR Apache-2.0"
description = "Renderer snapshots and fixed indexed drawing for Fluxel"
readme = "README.md"
repository = "https://github.com/fluxel-project/fluxel-renderer"
keywords = ["renderer", "graphics", "headless"]
categories = ["rendering", "graphics", "game-development"]

[features]
default = []
# Opt-in native upload coordination. The default crate remains the headless
# domain model and does not link a graphics backend.
gpu-upload = ["dep:fluxel-rendergraph", "dep:fluxel-rhi"]

[dependencies]
fluxel-rendergraph = { path = "../rendergraph", version = "0.8.0", optional = true }
fluxel-rhi = { path = "../rhi", version = "0.8.0", optional = true }

[dev-dependencies]
fluxel-rhi = { path = "../rhi", version = "0.8.0", default-features = false, features = ["dx12", "vulkan", "test-support"] }

[[example]]
name = "01_headless_frame"
required-features = ["gpu-upload"]
